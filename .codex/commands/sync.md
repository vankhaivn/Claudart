# Codex Sync Command

Sync CLAUDART configuration from the named source side into the active Codex side.

Usage:

```text
$sync claude
```

The argument is the source side. `$sync claude` means: read the current Claude snapshot and update Codex files.

This command is intended for downstream user projects after they have copied CLAUDART into their repository. In the CLAUDART base template itself, maintain `.claude` and `.codex/.agents` manually as peer source templates instead of using sync as an authoring shortcut.

## Hard Rules

1. Read `.claudart/sync-map.md` first and follow it as the source of truth.
2. Do not use `git diff`, commit history, or session history to infer changes. Sync from the current filesystem snapshot only.
3. Do not copy, convert, or duplicate `.claudart/CONTEXT.md` or `.claudart/JOURNAL.md`.
4. Never create `.claude/CONTEXT.md`, `.claude/JOURNAL.md`, `.codex/CONTEXT.md`, or `.codex/JOURNAL.md`.
5. Never import `@.claudart/JOURNAL.md` into `.claude/CLAUDE.md`, `AGENTS.md`, `.codex/CODEX.md`, `.claude/rules/`, or `.codex/guidelines/`.
6. Source wins only for generated target files. If a target file lacks `Generated by CLAUDART sync`, report it and ask before overwriting.
7. Bootstrap exception: in a downstream project's first Claude -> Codex migration, if the Codex side is still untouched CLAUDART scaffold with generic template wording and no project-specific Codex customizations, treat it as safe bootstrap target and overwrite it without extra approval.

## Procedure

### 1. Validate Invocation

- Accept only `claude` as the source when this command is running inside Codex.
- If the user asks for `$sync codex` from Codex, explain that Codex is already the active side and suggest running `/sync codex` from Claude Code instead.

### 2. Read Source Snapshot

Read these source files if present:

- `.claude/CLAUDE.md`
- `.claude/rules/*.md`
- `.claude/commands/*.md`
- `.claude/agents/*.md`

Also read `.claudart/CONTEXT.md` only to confirm shared state exists. Do not read `.claudart/JOURNAL.md` unless the user explicitly asks for history.

### 3. Convert To Codex Native Files

Update or create:

- `AGENTS.md`
- `.codex/CODEX.md`
- `.codex/guidelines/*.md`
- `.codex/commands/*.md`
- `.agents/skills/<command-name>/SKILL.md`
- `.codex/agents/*.toml`

Every generated target file must include a marker comment near the top:

```markdown
<!-- Generated by CLAUDART sync from <source-path>. Edit source side or run the opposite sync intentionally. -->
```

Before blocking on "manual target" conflicts, explicitly classify each target:

- `generated target`: already contains the sync marker -> overwrite from source.
- `bootstrap scaffold`: unmarked, but still generic CLAUDART template content copied from the base template -> overwrite from source and add the sync marker.
- `manual target`: unmarked and contains project-specific Codex instructions -> stop and ask before overwriting.

Typical bootstrap indicators:

- `AGENTS.md` or `.codex/CODEX.md` still describe the repository as the CLAUDART base template instead of the user's actual project.
- `.codex/commands/` and `.agents/skills/` still match the stock CLAUDART scaffold and do not mention the user's project.
- The meaningful project-specific rules, commands, or agents clearly exist only under `.claude/`.

### 4. Preserve Codex Requirements

- `AGENTS.md` must stay concise and point to `.codex/CODEX.md`, `.claudart/CONTEXT.md`, and `.codex/guidelines/*.md`.
- Codex command equivalents should be repo skills in `.agents/skills/*/SKILL.md`.
- `.codex/agents/*.toml` must include `name`, `description`, `model`, `sandbox_mode`, and `developer_instructions`.
- `.codex/rules/*.rules` is reserved for permission rules, not semantic coding guidelines.

### 5. Verify

Run text-level checks:

- Search for stale shared-memory paths outside sync policy/command text: `.claude/CONTEXT.md`, `.claude/JOURNAL.md`, `.codex/CONTEXT.md`, `.codex/JOURNAL.md`.
- Search for operational illegal imports outside guardrail text: `@.claudart/JOURNAL.md`.
- Confirm `.claudart/CONTEXT.md` and `.claudart/JOURNAL.md` exist.
- Confirm `.claudart/CONTEXT.md` is at most 150 lines.
- Confirm generated skills include `name:` and `description:`.
- Confirm generated Codex agents include `sandbox_mode`.

### 6. Report

Summarize:

1. Source side read.
2. Files created or updated.
3. Manual target files skipped.
4. Orphan candidates found.
5. Verification result.
