<!-- .codex/knowledge/INDEX.md — map of durable project knowledge. Maintained by $codex-checkpoint, $codex-learn, and $codex-refactor-memory; surfaced by $codex-start (which reads THIS file, never the detail files). Any skill that adds, renames, or removes a knowledge file MUST update this index in the same step. -->

# Project Knowledge

Durable, **descriptive** project facts — domain, architecture, glossary, integrations, and pointers to canonical docs that live in other folders. This is distinct from `.codex/guidelines/`, which is **prescriptive** behavior ("MUST/NEVER"). One topic per file; this index is the map — `$codex-start` reads it, then reads detail files on demand.

## Knowledge
<!-- One line per entry — keep this a map, not an encyclopedia:
- [Title](topic.md) — one-line hook · <type> · updated YYYY-MM-DD -->
_(none yet — entries are written by `$codex-checkpoint` when a durable fact surfaces, by `$codex-learn`, or by hand.)_

<!-- Topic-file frontmatter template:
---
name: <kebab-slug>
description: <one-line — shown in this index and used for recall>
type: domain | architecture | integration | glossary | reference | agent-context
sources:            # optional — reference-mode: point to canonical docs, do NOT duplicate them
  - ../../path/to/doc.md
  - https://...
related: ["[[other-knowledge]]", "[[a-guideline]]"]   # optional [[links]]
updated: YYYY-MM-DD
verify: "<optional staleness anchor, e.g. valid while services/payments/ exists>"
---
Raw facts; OR for reference-mode, 2-4 lines: what's in the source, when to read it, why it matters.
-->
